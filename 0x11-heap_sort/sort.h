#ifndef heap_sort_h
#define heap_sort_h

void heap_sort(int *array, size_t size);
void maximum_heap(int *array, int idx, int len, int size);
void swapper(int *a, int *b);
void print_array(const int *array, size_t size);

#endif
